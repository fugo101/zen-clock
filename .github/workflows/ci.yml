name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: [ self-hosted, Linux, X64 ]
    if: github.event_name == 'push' || github.event.pull_request.head.repo.fork == false
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: actions/cache@v4
        name: Cache PlatformIO toolchain
        with:
          path: ~/.platformio
          key: pio-tools-${{ runner.os }}-${{ hashFiles('platformio.ini', 'src/idf_component.yml') }}
          restore-keys: pio-tools-${{ runner.os }}-

      - uses: actions/cache@v4
        name: Cache SCons build cache
        with:
          path: .cache
          key: pio-build-${{ runner.os }}-${{ hashFiles('platformio.ini', 'sdkconfig.lilygo-t-display-s3') }}
          restore-keys: pio-build-${{ runner.os }}-

      - name: Setup PlatformIO PATH
        run: echo "$HOME/.platformio/penv/bin" >> $GITHUB_PATH

      - name: Check Python
        run: |
          if ! python3 --version 2>/dev/null; then
            echo "ERROR: python3 not found. Install it on the runner first."
            exit 1
          fi

      - name: Check PlatformIO
        run: |
          if ! pio --version 2>/dev/null; then
            echo "ERROR: pio not found. Run 'pip3 install platformio' on the runner first."
            exit 1
          fi

      - name: Build firmware
        run: pio run -e lilygo-t-display-s3

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ github.sha }}
          path: |
            .pio/build/lilygo-t-display-s3/firmware.bin
            .pio/build/lilygo-t-display-s3/partitions.bin
          retention-days: 7
